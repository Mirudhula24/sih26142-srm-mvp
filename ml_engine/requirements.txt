# Install torch matching your CUDA build first, e.g.
#   pip install torch==2.2.2 --index-url https://download.pytorch.org/whl/cu121
torch==2.2.2
timm==0.9.16
numpy==1.26.4
scipy==1.13.1
rasterio==1.3.10
affine==2.4.0
rio-cogeo==5.3.2
celery==5.4.0
redis==5.0.7
pydantic==2.7.4
pydantic-settings==2.3.4
pytest==8.2.2
# NOTE: pydensecrf is deliberately absent — it does not build on Python 3.10 / GCC 11.
# MRF smoothing is implemented natively in utils/mrf_smooth.py. See docs/TECH_CLASHES.md.
